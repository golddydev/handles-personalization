{
    "constructor_0": [
        {
            "treasury_fee": 1000000,      // Lovelace
            "treasury_address": "0x...",  // address in hex
            "pz_min_fee": 4000000,        // Lovelace
            "pz_providers": ["0x..."],    // address in hex
            "valid_contracts": ["0x..."], // payment credentials (ScriptKeyHash)
            "admin_creds": ["0x01AAF3"],  // PubKeyHash
            "settings_address": "0x..."   // address in hex
        }
    ]
}